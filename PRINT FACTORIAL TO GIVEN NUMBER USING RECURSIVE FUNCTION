#include <stdio.h>
#include<conio.h>
int rec(int);
void main()
{
    int n,fv;
    printf("enter n value");
    scanf("%d",&n);
    fv=rec(n);
    printf("factorial value is %d",fv);
}
int rec(int x)
{
    int f;
    if(x==1)
    return 1;
    else
    f=x*rec(x-1);
    return f;
    }
